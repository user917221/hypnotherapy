import { ReactNode } from "react";

export const metadata = {
    title: "Accompagnement IG Bas — Péguy Casteloot",
    description: "Rééquilibrage alimentaire et perte de poids sans frustration grâce à l'association de l'indice glycémique bas et de l'hypnose.",
};

export default function IgBasLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
